import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Ghost Reef's wave-8 worker. Build the recovery:
 * bleached coral stands (the coral idiom, bone palette) with returning
 * colour deepening toward the far end. Draw only from
 * `SEEDS.wingGhostReef` (and `^` substreams).
 */
export function buildGhostReefFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
