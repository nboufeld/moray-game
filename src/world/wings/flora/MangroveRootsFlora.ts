import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Mangrove Roots' wave-8 worker. Build the shelter:
 * prop-root column maze, amber dapple accents, close warm dressing. Roots
 * may join colliders via the wing-worker's own module if swim-through
 * blocking is wanted — flag it in the ledger. Draw only from
 * `SEEDS.wingMangroveRoots` (and `^` substreams).
 */
export function buildMangroveRootsFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
