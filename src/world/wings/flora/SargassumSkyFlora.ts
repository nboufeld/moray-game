import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Sargassum Sky's wave-8 worker. Build the ceiling:
 * the golden weed canopy overhead (the kelp-canopy idiom, hung high),
 * amber light-patch accents, a floor kept deliberately simple. Draw only
 * from `SEEDS.wingSargassumSky` (and `^` substreams).
 */
export function buildSargassumSkyFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
