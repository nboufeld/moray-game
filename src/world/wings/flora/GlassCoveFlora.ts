import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Sea-Glass Cove's wave-8 worker. Build the toybox:
 * instanced glass-pebble drifts in pastel aquas and pinks (the rubble
 * idiom with translucent-reading toon tints), glint accents. Draw only
 * from `SEEDS.wingGlassCove` (and `^` substreams).
 */
export function buildGlassCoveFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
