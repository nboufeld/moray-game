import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Ruins Terrace's wave-8 worker. Build the age:
 * mossed arches and fallen columns (lathed stone idiom), gold-green
 * dressing on stepped shelves. Scenery only. Draw only from
 * `SEEDS.wingRuinsTerrace` (and `^` substreams).
 */
export function buildRuinsTerraceFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
