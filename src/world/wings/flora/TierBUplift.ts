/**
 * Batch 4 (MASTER §4 "closure") — the Tier B side-room uplift's shared
 * contract, stated once so ten flora files and three test suites agree on
 * one name:
 *
 * - Every Tier B wing mounts ONE group with this name, appended after
 *   every wave-8 draw and before the gate veil, holding the wing's T1
 *   ground statement (a kit carpet/litter in the wing's own palette).
 * - The veil rides `GateVeilMount` exactly as the gateway wings' do, but
 *   stands at the wing's GATE (doorR ≈ 32) with inks sourced from the
 *   wing itself — the doorway a side room shows the bowl is a promise of
 *   the room, the way a gateway's shows its province.
 * - Budget ceiling per MASTER R2: ≤ +6 draws / ≤ 20k tris per Tier B
 *   wing, measured in `tests/wingsTierB.test.ts`; reroll fence proved
 *   there against pre-uplift sentinels.
 */
export const TIERB_GROUP_NAME = "wing-uplift-tierb";
