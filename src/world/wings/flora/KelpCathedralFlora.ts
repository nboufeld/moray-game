import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Kelp Cathedral's wave-8 worker. Build the vault:
 * giant kelp columns (see `Kelp.ts`'s giants for the idiom), shaft-light
 * accents, floor moss. Draw only from `SEEDS.wingKelpCathedral` (and its
 * `^` substreams); sample `seabedHeight` for every foot.
 */
export function buildKelpCathedralFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
