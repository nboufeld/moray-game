import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Vent Springs' wave-8 worker. Build the heat: mineral
 * chimneys, shimmer columns (the bubble-vent idiom), amber glow accents on
 * a charcoal floor. Draw only from `SEEDS.wingVentSprings` (and `^`
 * substreams).
 */
export function buildVentSpringsFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
