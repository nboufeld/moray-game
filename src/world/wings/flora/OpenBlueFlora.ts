import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Open Blue's wave-8 worker. Build the vastness —
 * which is to say, almost nothing: the drop-off's lip, distant silhouette
 * accents, and restraint. The emptiness is the design. Draw only from
 * `SEEDS.wingOpenBlue` (and `^` substreams).
 */
export function buildOpenBlueFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
