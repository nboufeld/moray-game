import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Sandfall Dunes' wave-8 worker. Build the quiet:
 * tall dune wall dressing, slow sandfall ribbons over their lips (the
 * particle/billboard idiom at low speed), warm neutral accents. Draw only
 * from `SEEDS.wingSandfallDunes` (and `^` substreams).
 */
export function buildSandfallDunesFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
