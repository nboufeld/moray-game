import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Nursery Shallows' wave-8 worker. Build the terrace:
 * baby coral scatters, bright sand accents, small anemone patches. Draw
 * only from `SEEDS.wingNurseryShallows` (and `^` substreams); sample
 * `seabedHeight` for every foot.
 */
export function buildNurseryShallowsFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
