import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Current Run's wave-8 worker. Build the rush: grass
 * banners bent one way, streaming bubble lines, channel dressing. Draw
 * only from `SEEDS.wingCurrentRun` (and `^` substreams).
 */
export function buildCurrentRunFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
