import { Group } from "three";
import type { WingDef, WingFlora } from "../WingTypes";

/**
 * STUB — owned by the Ice Grotto's wave-8 worker. Build the hush: crystal
 * spires, frost-pale flora, a low roof's under-dressing. Draw only from
 * `SEEDS.wingIceGrotto` (and `^` substreams).
 */
export function buildIceGrottoFlora(_def: WingDef): WingFlora {
  return { group: new Group() };
}
